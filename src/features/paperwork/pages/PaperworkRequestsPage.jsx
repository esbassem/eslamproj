import { useDeferredValue, useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';
import { REQUEST_FILTERS } from '@/features/paperwork/adapters/paperworkViewModels';
import { paperworkReadService } from '@/features/paperwork/services/queries/paperworkRead.service';
import { usePaperworkQuery, usePaperworkTenant } from '@/features/paperwork/hooks/usePaperworkQuery';
import { PaperworkPage } from '@/features/paperwork/shared/PaperworkPage';
import { EmptyState, FilterBar, PageError, PageSkeleton, SearchInput, SerialDisplay, StatusBadge } from '@/features/paperwork/shared/PaperworkUI';
import { PAPERWORK_ROUTES } from '@/features/paperwork/routes/paperworkRoutes';

export function PaperworkRequestsPage() {
  const tenantId = usePaperworkTenant();
  const [params, setParams] = useSearchParams();
  const filter = params.get('filter') || 'all';
  const page = Math.max(Number(params.get('page')) || 0, 0);
  const [search, setSearch] = useState(params.get('q') || '');
  const deferredSearch = useDeferredValue(search);
  const query = usePaperworkQuery(() => tenantId ? paperworkReadService.listRequestSummaries({ tenantId, filter, search: deferredSearch, page }) : Promise.resolve(null), [tenantId, filter, deferredSearch, page]);
  const update = (next) => setParams((current) => { Object.entries(next).forEach(([key, value]) => value ? current.set(key, value) : current.delete(key)); return current; });
  return <PaperworkPage title="الطلبات" description="متابعة دورة طلبات أوراق الملكية."><div className="mb-4 flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-3 sm:flex-row"><SearchInput value={search} onChange={(value) => { setSearch(value); update({ q: value, page: '' }); }} placeholder="بحث بالعميل أو صاحب الورق أو المنتج أو الرقم أو الجهة" /><FilterBar filters={REQUEST_FILTERS} value={filter} onChange={(value) => update({ filter: value === 'all' ? '' : value, page: '' })} /></div>{query.loading ? <PageSkeleton /> : query.error ? <PageError message={query.error} onRetry={query.retry} /> : !query.data.items.length ? <EmptyState title="لا توجد طلبات في هذه الحالة." /> : <><div className="hidden overflow-hidden rounded-2xl border border-slate-200 bg-white md:block"><table className="w-full text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-4">الحالة</th><th>العميل وصاحب الورق</th><th>المنتج والأرقام</th><th>جهة الإصدار</th><th>آخر تحديث</th></tr></thead><tbody className="divide-y divide-slate-100">{query.data.items.map((item) => <tr key={item.id} className="hover:bg-blue-50/40"><td className="p-4"><Link to={PAPERWORK_ROUTES.requestDetails(item.id)}><StatusBadge label={item.stageLabel} status={item.currentStage} /></Link></td><td><div className="font-black">{item.customerName}</div>{item.ownerName && item.ownerName !== item.customerName ? <div className="text-xs text-slate-500">صاحب الورق: {item.ownerName}</div> : null}</td><td><div className="font-bold">{item.productName}</div><SerialDisplay trackingNumber={item.trackingNumber} identifiers={item.identifiers} /></td><td className="font-bold text-slate-600">{item.processorName}</td><td className="text-xs text-slate-500">{new Date(item.updatedAt).toLocaleString('ar-EG')}</td></tr>)}</tbody></table></div><div className="space-y-3 md:hidden">{query.data.items.map((item) => <Link key={item.id} to={PAPERWORK_ROUTES.requestDetails(item.id)} className="block rounded-2xl border border-slate-200 bg-white p-4"><div className="flex items-start justify-between gap-3"><div><div className="font-black">{item.customerName}</div><div className="mt-1 text-sm font-bold text-slate-600">{item.productName}</div></div><StatusBadge label={item.stageLabel} status={item.currentStage} /></div><div className="mt-3"><SerialDisplay trackingNumber={item.trackingNumber} identifiers={item.identifiers} /></div><div className="mt-3 text-xs font-bold text-slate-500">{item.processorName}</div></Link>)}</div><div className="mt-5 flex items-center justify-between"><button disabled={!page} onClick={() => update({ page: String(page - 1) })} className="rounded-lg border bg-white px-4 py-2 text-sm font-black disabled:opacity-40">السابق</button><span className="text-xs font-bold text-slate-500">{query.data.count} طلب</span><button disabled={(page + 1) * query.data.pageSize >= query.data.count} onClick={() => update({ page: String(page + 1) })} className="rounded-lg border bg-white px-4 py-2 text-sm font-black disabled:opacity-40">التالي</button></div></>}
  </PaperworkPage>;
}
